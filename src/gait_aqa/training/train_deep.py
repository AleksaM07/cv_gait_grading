"""Optional deep-video baseline stub."""

from __future__ import annotations


def train_deep() -> None:
    """Raise a clear message because the deep baseline is optional."""
    raise NotImplementedError(
        "The deep-video baseline is intentionally optional and should be run "
        "after the classical baseline has real rendered MuJoCo clips."
    )
